daemon_options = clean

terminal {
    title = float_terminal
    command = rio --title-placeholder float_terminal
    options = persist
}

monitoring {
    title = monitoring
    command = rio --title-placeholder monitoring -e btm
    options = persist
}

subtui {
    title = subtui
    command = rio --title-placeholder subtui -e subtui
    options = persist
}

wlctl {
    title = wlctl
    command = rio --title-placeholder wlctl -e wlctl
    options = persist
}

yazi {
    title = yazi
    command = rio --title-placeholder yazi -e yazi
    options = persist
}

mxr {
    title = mxr
    command = rio --title-placeholder mxr -e mxr
    options = persist
}
