daemon_options = clean

terminal {
    title = float_terminal
    command = rioterm --title-placeholder float_terminal
    options = persist
}

monitoring {
    title = monitoring
    command = rioterm --title-placeholder monitoring -e btm
    options = persist
}

subtui {
    title = subtui
    command = rioterm --title-placeholder subtui -e subtui
    options = persist
}

wlctl {
    title = wlctl
    command = rioterm --title-placeholder wlctl -e wlctl
    options = persist
}

yazi {
    title = yazi
    command = rioterm --title-placeholder yazi -e yazi
    options = persist
}

mxr {
    title = mxr
    command = rioterm --title-placeholder mxr -e mxr
    options = persist
}
